# Shipped fallback text for this module's exception (English), contributed to the shared problem
# pipeline as a ProblemMessageBundle - see AuditAutoConfiguration.
#
# One key, because this module raises one exception at a caller. The text says the change did not
# happen, which is the fact the client needs: an audit row that could not be written fails the
# operation it describes, so a retry is correct and a resubmission is not a duplicate.

ludwig.audit.error.write-failed.title=Temporarily unavailable
ludwig.audit.error.write-failed=The change was not applied because it could not be recorded in the audit trail. Nothing was modified. Please try again; if it keeps happening, quote the trace id from this response.
