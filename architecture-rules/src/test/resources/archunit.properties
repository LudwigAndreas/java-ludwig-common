# Keeps the frozen-violation store of FreezeTest inside the build directory instead of the module
# root. Only the freeze.* keys are set, so nothing else about the importer changes.
freeze.store.default.path=target/archunit_store
freeze.store.default.allowStoreCreation=true
