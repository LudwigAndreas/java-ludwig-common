<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>ru.ludwigandreas</groupId>
        <artifactId>common</artifactId>
        <version>${revision}</version>
    </parent>

    <artifactId>file-ingest-spring-boot-starter</artifactId>
    <packaging>jar</packaging>

    <name>File ingest Spring Boot starter</name>
    <description>
        The once-a-day bulk file ingest: reads a large object out of storage, parses it record by
        record, writes the records into the database, and tells somebody it is done - without ever
        holding the file in memory and without ever losing a record. Arrival detection before the
        first byte is read, ranged streaming with a checkpoint committed in the same transaction as
        the data it describes, batches bounded by count and by bytes, quarantine with a threshold
        policy, a balance check that gates COMPLETED, receipts, archival, metrics including the file
        that never came, an audit SPI and an actuator endpoint.
    </description>

    <!--
        A library module: parented by the reactor root, importing ludwig-bom. Deliberately NOT
        ludwig-service-parent - see the note in that file.
    -->

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>ru.ludwigandreas</groupId>
                <artifactId>ludwig-bom</artifactId>
                <version>${project.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!--
            The platform's audit envelope and sink. This module's IngestAuditLogger and its SLF4J
            implementation are gone into it; IngestAuditEvent stays as the authoring surface, with its
            nine action constants, and gains toAuditEvent().
        -->
        <dependency>
            <groupId>ru.ludwigandreas</groupId>
            <artifactId>audit-core</artifactId>
        </dependency>

        <!--
            NOT optional. The whole point of this module is that it reads the object through the
            platform's one ObjectStore rather than through a bucket client of its own - writing a
            second client here is the JdbcRunLock mistake, and an optional dependency would make it
            the path of least resistance.
        -->
        <dependency>
            <groupId>ru.ludwigandreas</groupId>
            <artifactId>object-storage-spring-boot-starter</artifactId>
        </dependency>

        <!-- job-core: the cron schedule, the self-scheduling lifecycle, and the platform's one
             leased RunLock with the lease renewal this module's batch loop depends on. -->
        <dependency>
            <groupId>ru.ludwigandreas</groupId>
            <artifactId>job-core</artifactId>
        </dependency>

        <!-- db-core: GeneratedEntity/BaseRepository and the auditing infrastructure. -->
        <dependency>
            <groupId>ru.ludwigandreas</groupId>
            <artifactId>db-core</artifactId>
        </dependency>

        <!-- NOT optional, for the reason the export starter gives: every failure a caller can see
             is a LocalizedException rendered by web-core's one ProblemDetail pipeline. -->
        <dependency>
            <groupId>ru.ludwigandreas</groupId>
            <artifactId>web-core-spring-boot-starter</artifactId>
        </dependency>

        <!-- Optional: "the ingest finished" is published as an outbox event, so the event and the
             run's COMPLETED status commit together. A service that nobody listens to does not need
             it. -->
        <dependency>
            <groupId>ru.ludwigandreas</groupId>
            <artifactId>outbox-spring-boot-starter</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Hibernate + Spring Data JPA + jakarta.persistence-api -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-context</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-autoconfigure</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>

        <!--
            Every query this module writes against its own tables is a compile-time-checked QueryDSL
            expression. The one exception is ru.ludwigandreas.ingest.bulk - see that package and the
            "Module conventions" section of CLAUDE.md for the carve-out and its boundary, which an
            ArchUnit rule in this module's own test suite enforces.
        -->
        <dependency>
            <groupId>com.querydsl</groupId>
            <artifactId>querydsl-core</artifactId>
        </dependency>
        <dependency>
            <groupId>com.querydsl</groupId>
            <artifactId>querydsl-jpa</artifactId>
            <classifier>jakarta</classifier>
        </dependency>

        <!-- The receipt object is JSON, and so is the run summary an outbox event carries. -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.datatype</groupId>
            <artifactId>jackson-datatype-jsr310</artifactId>
        </dependency>

        <!-- Plug-and-play with Postgres, and the source of CopyManager: see the bulk package. -->
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>provided</scope>
        </dependency>

        <!-- Optional: schema delivered as a Liquibase changelog, applied only if the consumer uses
             Liquibase and has not folded it into its own master history. -->
        <dependency>
            <groupId>org.liquibase</groupId>
            <artifactId>liquibase-core</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Optional: run, record, batch and merge metrics, plus the missing-file gauge. -->
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-core</artifactId>
            <optional>true</optional>
        </dependency>

        <!--
            Optional: the `fileingest` actuator endpoint. Both artifacts, because they carry different
            halves of what an endpoint needs: `spring-boot-actuator` has @Endpoint and the operation
            annotations, and `spring-boot-actuator-autoconfigure` has @ConditionalOnAvailableEndpoint,
            which is what keeps the bean from being created for an endpoint the estate has not exposed.
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-actuator</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-actuator-autoconfigure</artifactId>
            <optional>true</optional>
        </dependency>

        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-testcontainers</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>postgresql</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- The S3 half of the integration suite: see object-storage's README for why the container
             is LocalStack rather than MinIO. -->
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>localstack</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>s3</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>apache-client</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>auth</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>com.tngtech.archunit</groupId>
            <artifactId>archunit</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>ru.ludwigandreas</groupId>
            <artifactId>architecture-rules</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </path>
                        <path>
                            <groupId>com.querydsl</groupId>
                            <artifactId>querydsl-apt</artifactId>
                            <classifier>jakarta</classifier>
                        </path>
                        <path>
                            <groupId>jakarta.persistence</groupId>
                            <artifactId>jakarta.persistence-api</artifactId>
                        </path>
                    </annotationProcessorPaths>
                    <annotationProcessors>
                        <annotationProcessor>lombok.launch.AnnotationProcessorHider$AnnotationProcessor</annotationProcessor>
                        <annotationProcessor>com.querydsl.apt.jpa.JPAAnnotationProcessor</annotationProcessor>
                    </annotationProcessors>
                </configuration>
            </plugin>
            <!--
                Failsafe, for the same reason job-core and the storage module bind it: this module's
                integration suite starts two containers and generates a 200MB file, which is not
                something `mvn test` should do on every build of every module that depends on it.
                Surefire's default includes never match `*IT.java`.
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
                <executions>
                    <execution>
                        <id>integration-tests</id>
                        <goals>
                            <goal>integration-test</goal>
                            <goal>verify</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
